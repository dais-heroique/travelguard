from pathlib import Path
p=Path('/home/ubuntu/travelguard/scripts/generate_native_ios.py')
s=p.read_text()
s=s.replace('(store.location.accuracy ?? 999) > 100 ? "Position trop imprécise pour calculer les distances" : nearbyRisks.isEmpty ? "Aucun risque connu dans un rayon de 10 km"', '(store.location.accuracy ?? 999) > 200 ? "Position très imprécise : distances masquées" : (store.location.accuracy ?? 999) > 50 ? "Position approximative : distances indicatives" : nearbyRisks.isEmpty ? "Aucun risque connu dans un rayon de 10 km"',1)
s=s.replace('Text("Aucune donnée géolocalisée autorisée n’est disponible dans un rayon de 10 km. Les sources officielles générales restent consultables dans Sécurité.")', 'Text(store.location.coordinate == nil ? "Localisation en cours…" : (store.location.accuracy ?? 999) > 200 ? "Position très imprécise…" : trustedRisks.isEmpty ? "Impossible de charger les risques : aucune base géolocalisée autorisée." : "Aucun risque connu dans un rayon de 10 km. Cela ne signifie pas que la zone est sans risque.")',1)
s=s.replace('HStack(spacing: 8) { Button { zoom(by: 0.55) } label: { Image(systemName: "plus").frame(width: 38, height: 38) }; Button { zoom(by: 1.8) } label: { Image(systemName: "minus").frame(width: 38, height: 38) } }.font(.headline).foregroundStyle(TGColor.ink).background(.ultraThinMaterial).clipShape(Capsule()).padding(.leading, 18).padding(.bottom, 28)', 'HStack(spacing: 8) { Button { recenter() } label: { Image(systemName: "location.fill").frame(width: 38, height: 38) }.accessibilityLabel("Recentrer sur ma position"); Button { zoom(by: 0.55) } label: { Image(systemName: "plus").frame(width: 38, height: 38) }.accessibilityLabel("Zoomer"); Button { zoom(by: 1.8) } label: { Image(systemName: "minus").frame(width: 38, height: 38) }.accessibilityLabel("Dézoomer") }.font(.headline).foregroundStyle(TGColor.ink).background(.ultraThinMaterial).clipShape(Capsule()).padding(.leading, 18).padding(.bottom, 28)',1)
s=s.replace('struct FullScreenRiskMapView: View {\n    @Environment(\\.dismiss) private var dismiss', 'struct FullScreenRiskMapView: View {\n    @EnvironmentObject private var store: TravelGuardStore\n    @Environment(\\.dismiss) private var dismiss',1)
s=s.replace('    private func zoom(by factor: Double) { region.span =', '    private func recenter() { guard let coordinate = store.location.coordinate else { store.location.requestPermission(); return }; region.center = coordinate; region.span = MKCoordinateSpan(latitudeDelta: 0.035, longitudeDelta: 0.035) }\n    private func zoom(by factor: Double) { region.span =',1)
p.write_text(s)
print('finished audit 12 map patch')
